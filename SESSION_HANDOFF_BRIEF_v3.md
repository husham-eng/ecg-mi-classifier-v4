# ملف تسليم الجلسة v3 — مشروع ecg-mi-classifier

**تاريخ هذا التحديث**: سبتمبر 2026
**الملف السابق**: `SESSION_HANDOFF_BRIEF_v2.md` (بداية هذي الجلسة الطويلة)

---

## 0. القرار الأهم الذي يحكم كل شيء آخر بهذا الملف

**`ecg-mi-classifier-v3` مجمَّد بالكامل** (لا Commits، لا Push، لا أي
تعديل مهما كان بسيطاً) — لأن رابط المستودع بالضبط موثَّق بورقة علمية
مُرسَلة لمجلة *Biomedical Signal Processing and Control* (Elsevier)، ولسا
تحت المراجعة. أي تعديل عليه الآن يكسر التطابق بين الموثَّق بالورقة
والمنشور فعلياً.

**كل التطوير النشط منذ بداية هذي الجلسة يحصل حصراً على `ecg-mi-classifier-v4`**
— مستودع وتطبيق وأرشفة Zenodo مستقلة بالكامل عن v3، ودراسة مقارنة
مصنِّفات مستقلة تماماً لا تذكر v3 إطلاقاً.

**لا تلمس v3 بأي طريقة حتى يصلك قرار نهائي من المجلة.**

---

## 1. ملخّص تنفيذي — ماذا تم بهذي الجلسة (بالترتيب الزمني)

### 1.1 إنشاء البنية التحتية المستقلة لـv4
- مستودع GitHub جديد: `github.com/husham-eng/ecg-mi-classifier-v4` (نسخة
  مستقلة تماماً من v3، تاريخ Git منفصل، لا Fork).
- خدمة Render جديدة موازية: `ecg-mi-classifier-v4.onrender.com` (خطة
  مجانية)، بمتغيرات بيئة SendGrid منفصلة.
- **GitHub Release**: `v1.0.1` (أحدث وسم، بعد تفعيل Webhook Zenodo).
- **Zenodo DOI مستقل**: `https://doi.org/10.5281/zenodo.22670284`
  (مستقل تماماً عن أي DOI خاص بـv3).

### 1.2 تبسيط واجهة المستخدم النهائي (v4 فقط)
- `templates/index.html`: استبدال عرض JSON الخام باستجابة `/classify`
  ببطاقة نتيجة مبسّطة (اسم الفئة الكامل + شريط ثقة ملوّن)، مع تفاصيل
  كاملة قابلة للطي خلف زر اختياري "عرض التفاصيل الكاملة".
- `translations.py`: أسماء فئات كاملة مقروءة (عربي/إنجليزي/روسي) —
  Normal / Anterior MI / Antero-Septal MI / Infero-Lateral MI /
  Infero-Postero-Lateral MI.
- `ecg_pipeline/email_report.py`: بطاقة ملخّص بصرية بارزة أعلى تقرير
  الإيميل (نفس مبدأ الواجهة)، مع إبقاء كل الجداول والأشكال التفصيلية
  كما هي بالأسفل.

### 1.3 تجربة أقرب سنترويد (نتيجة سلبية موثَّقة)
- `tools/nearest_centroid_experiment_colab.py` — قارن Random Forost
  الحالي بمصنِّف أقرب سنترويد (سنترويد واحد/فئة، Stemming بالتقاطع).
- **النتيجة**: أقرب سنترويد لم يتفوّق على أي قطب. **القرار: البقاء على
  Random Forest.**

### 1.4 مقارنة موسَّعة بست عائلات تصنيف (الدراسة الرئيسية لهذي الجلسة)
- `tools/classifier_comparison_experiment_colab.py` — يقارن Random
  Forest، شجرة قرار مفردة، SVM (RBF)، أقرب سنترويد، نماذج عنقودية
  (K-Means)، واختزال أبعاد (PCA)+انحدار لوجستي — كلها على **نفس** التغذية
  (Stemming بالتقاطع) ونفس GroupKFold (5 طيّات، مستوى المريض).
- `tools/diagnostics_experiment_colab.py` — سكربت تشخيصي إضافي: نسبة
  تخفيض Stemming، مصفوفات ارتباك Out-of-Fold حقيقية، رسم توضيحي لمراحل
  معالجة نبضة واحدة.

**⚠️ خطأ اكتُشِف وصُحِّح أثناء هذي التجربة**: أقطاب aVR/V2/V6 كانت تتدرّب
وتُختبَر على فئة "A" رغم عدم دعمها تشخيصياً بهذي الأقطاب (بيانات المريض
موجودة بكل الأقطاب الأربعة معاً بحكم التسجيل المتزامن). تم تصحيح الفلترة
بالسكربتات الثلاثة. **الأرقام النهائية أعلى بكثير من النسخة الملوَّثة**
(مثال: Random Forest على V2 من 52.9% إلى 60.8% Macro-F1).

**النتيجة النهائية (بعد التصحيح، الترتيب العام بمتوسط الأقطاب الأربعة)**:
1. Random Forest — 0.483 (الأفضل على كل قطب بلا استثناء)
2. شجرة قرار مفردة — 0.458
3. SVM (RBF) — 0.452 (متقارب جداً مع شجرة القرار)
4. PCA + انحدار لوجستي — 0.416
5. نماذج عنقودية (K-Means) — 0.395
6. أقرب سنترويد — 0.394

**ملاحظة أهم من الترتيب نفسه**: حتى أفضل طريقة (RF) لا تتجاوز 60.8%
Macro-F1 — يعني **المشكلة الحقيقية ليست اختيار خوارزمية التصنيف**، بل
حجم بيانات التدريب المحدود (خصوصاً فئة IPL النادرة، ضعيفة الاكتشاف بـ6%
recall على 3 من 4 أقطاب بغض النظر عن الطريقة).

### 1.5 المقالة العلمية الكاملة (إنجليزي)
`docs/classifier_comparison_article_EN.docx` — مقالة كاملة تحتوي:
- أهداف صريحة (توسيع تجربة ثنائية لمقارنة منهجية شاملة + تبرير GroupKFold
  لتجنّب تسريب البيانات).
- **قسم "دراسات سابقة" حقيقي** (1.1 Related Work) يستشهد بـ20 مرجعاً
  حقيقياً برقمها `[1]`-`[20]` داخل النص (بيانات PTB-XL، تصنيف MI كلاسيكي،
  تعلّم عميق، استخراج القمم R، الأسس النظرية للطرق الست).
- شرح مبسّط لخطوات المعالجة **مدمَج داخل قسم المنهجية نفسه** (لا قسماً
  منفصلاً).
- 6 جداول و4 أشكال، **كلها مُرقَّمة ومُشار إليها بالنص** (Table 1-6،
  Figure 1-4).
- مصفوفات ارتباك حقيقية (Out-of-Fold) مبنية على الأرقام المصحَّحة.
- قسم "Data and Code Availability" — v4 فقط (لا ذكر لـv3 إطلاقاً)، رابط
  المستودع، التطبيق الحي، DOI الفعلي.
- **بلا اسم كاتب/جهة ظاهرة** (بطلب صريح) — فقط اسم المشروع والتاريخ
  بصفحة العنوان، وخصائص الملف الفعلية (Title/Subject/Creator) بلا اسم
  شخصي.

⚠️ **النسخة العربية من المقالة (`docs/classifier_comparison_article.docx`)
لسا بالنسخة القديمة** — تحتوي الأرقام **قبل** تصحيح مشكلة فئة A، وبلا أي
من التحسينات الهيكلية (دراسات سابقة، جداول مرقَّمة، إلخ). **هذي أولوية
معلَّقة صريحة للجلسة القادمة إذا احتجتها.**

### 1.6 حزمة "تحسينات v4" جاهزة للتطبيق على v3 لاحقاً
`v4_improvements_package.zip` (سلَّمته لك بجلسة سابقة، احتفظ به) يحتوي:
- `v4_improvements.patch` — فرق Git كامل بين v3 الأصلية وv4 الحالية،
  **مُختبَر فعلياً** (تحقّق محلي: يُطبَّق بلا أي تعارض على نسخة نظيفة من v3).
- `HOW_TO_APPLY.md` — تعليمات تطبيق بـ3 أوامر Git، وقت ما يصير مسموحاً.

⚠️ **هذا الملف يحتاج تحديث** ليشمل كل ما تم بعده بهذي الجلسة (تصحيح فئة
A، المقالة الإنجليزية الكاملة، DOI). **راجع القسم 3 أدناه.**

---

## 2. الوضع الحالي للمستودعين (لقطة سريعة)

| | v3 | v4 |
|---|---|---|
| الحالة | 🔒 مجمَّد تماماً | 🟢 نشط، أحدث Commit: تسمية الجداول/الأشكال |
| DOI | (خاص بالورقة، لا تلمسه) | `10.5281/zenodo.22670284` |
| التطبيق | `ecg-mi-classifier.onrender.com` | `ecg-mi-classifier-v4.onrender.com` |
| المصنِّف | Random Forest (كما هو) | Random Forest (نفسه، مؤكَّد بعد مقارنة 6 طرق) |
| الواجهة | JSON خام | بطاقة نتيجة مبسّطة |
| التوثيق | ثابت (وقت التجميد) | يشمل تجربتين + مقالة إنجليزية كاملة |

---

## 3. أولويات الجلسة القادمة (بترتيب ذكرك، الأحدث أولاً)

### أولوية 1 — استكشاف تعلّم عميق لتحسين اكتشاف الأقطاب من صور ECG
**الفكرة**: التطبيق حالياً (ميزة "الرفع اللوحي" اللي شفناها بالجلسات
السابقة — كشف تلقائي لموقع الأقطاب من صورة ورقة ECG مطبوعة) يعتمد على
منطق اكتشاف حالي (على الأغلب معالجة صور كلاسيكية: كشف حواف/شبكة/منحنيات).
**المطلوب استكشافه**: استبدال أو تعزيز هذا الجزء بشبكة عصبية/تعلّم عميق
لتحسين:
1. **دقة اكتشاف موقع كل قطب** داخل صورة اللوحة (Object Detection/Segmentation
   لموقع كل مخطط قطب على الورقة).
2. **تحويل الصورة إلى إشارة رقمية** (Image-to-Signal / Waveform Digitization)
   بجودة تناسب باقي الـPipeline من ناحية معدل العينات (`fs`) والتردد —
   يعني الخرج يكون متوافقاً مباشرة مع `preprocessing.py` الحالي (نفس
   `pre=100, post=300`، نفس منطق R-peak، إلخ) بدل الحاجة لتحويل إضافي.

**خطوات مقترحة للجلسة القادمة**:
- مراجعة الكود الحالي لميزة "الرفع اللوحي" (`panel_detecting` بـ
  `templates/index.html`، والدالة المقابلة بـ`app.py`) لفهم آلية الكشف
  الحالية بالضبط ونقاط ضعفها الموثَّقة (إن وُجدت شكاوى/حالات فشل).
- بحث سريع عن نماذج جاهزة/أوراق علمية لتحويل صور ECG المطبوعة لإشارة
  رقمية (هذا مجال بحثي نشط، فيه على الأرجح حلول مفتوحة المصدر جاهزة
  يمكن البناء عليها بدل التدريب من الصفر).
- تحديد: هل التحسين يشمل v4 فقط (الأسهل بما إنه نشط) أم يُدرَج ضمن حزمة
  التحسينات المستقبلية لـv3 أيضاً بعد انتهاء المراجعة.

### أولوية 2 — تطبيق كل تحسينات v4 على v3 (بعد انتهاء مراجعة المجلة)
**الحالة**: حزمة `v4_improvements_package.zip` جاهزة (راجع القسم 1.6)،
لكن **تحتاج تحديث** لتشمل:
- تصحيح فئة A (aVR/V2/V6).
- المقالة الإنجليزية الكاملة النهائية.
- أي تحسين إضافي من "أولوية 1" أعلاه إذا اكتمل قبلها.

**خطوة مقترحة للجلسة القادمة**: توليد `v4_improvements.patch` جديد
(بنفس طريقة `git diff <commit البداية> HEAD --binary`) يغطي كل Commits
هذي الجلسة، واختباره محلياً على نسخة نظيفة من v3 (بلا Push فعلي، بما إن
v3 لسا مجمَّداً)، وتسليمه لك كملف محدَّث.

**⚠️ تذكير**: هذي الخطوة **لا تُنفَّذ فعلياً** (لا Push لـv3) إلا بعد
تأكيدك الصريح أن مراجعة المجلة انتهت.

---

## 4. ملفات مرجعية مهمة لبداية الجلسة القادمة

- **هذا الملف** (`SESSION_HANDOFF_BRIEF_v3.md`) — نقطة البداية.
- `v4_improvements_package.zip` — عندك محفوظ، يحتاج تحديث (أولوية 2).
- مستودع v4 نفسه: `README.md` فيه كل تفاصيل التجارب والقرارات بالتوثيق
  الكامل (أدق من هذا الملخّص).
- `docs/classifier_comparison_article_EN.docx` بمستودع v4 — المرجع
  العلمي الكامل والنهائي لتجربة الست طرق.
