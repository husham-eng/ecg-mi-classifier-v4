__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/
env/
*.egg-info/
.DS_Store
*.log
instance/
.env

# ملف بيانات مؤقت واحد كبير الحجم من tools/ (يُعاد توليده من بيانات PTB-XL
# الخام) — تحديد الاسم صراحة (لا نمط عام *.pkl) لتفادي استبعاد ملفات
# النماذج المدرَّبة الفعلية بالخطأ (models/*/model.pkl ضرورية للتطبيق!).
ptbxl_beats.pkl

# سجل محاولات لوحة التحكم — قد يحوي صوراً حساسة، لا يُرفع لمستودع عام إطلاقاً
logs/

# ملفات محلية لأي منصة نشر
.pythonanywhere/
